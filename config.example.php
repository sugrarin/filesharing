<?php

/*
 * Optional: copy this file to config.php, set a unique password hash, and keep
 * config.php out of version control. This value is used only when the first
 * admin account is created. Without it, the built-in DemoPassword is used.
 */
define('FILESHARE_ADMIN_PASSWORD_HASH', 'REPLACE_WITH_A_PASSWORD_HASH');
